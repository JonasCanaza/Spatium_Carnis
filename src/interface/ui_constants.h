#pragma once

namespace UIConstants
{
    // BUTTON

    const float BUTTON_WIDTH = 350.0f;
    const float BUTTON_HEIGHT = 65.0f;
    const float BUTTON_MARGIN_Y = 10.0f;

    // TITLE

    const float TITLE_WIDTH = 650.0f;
    const float TITLE_HEIGHT = 300.0f;
    const float TITLE_TO_BUTTONS_MARGIN = 40.0f;

    // LOGO

    const float LOGO_WIDTH = 600.0f;
    const float LOGO_HEIGHT = 350.0f;
    const float LOGO_TO_BUTTONS_MARGIN = 40.0f;

    // TEXT INTERFACE

    const int USER_INTERFACE_FONT_SIZE = 30;
}